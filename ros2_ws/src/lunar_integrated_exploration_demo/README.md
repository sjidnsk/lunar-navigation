# 复杂地形完整探索 demo

使用正式地图、探索、导航和轮式控制模块；本包提供合成观测、车辆模拟和显示。
默认 300 m 场景、0.2 m fine / 1 m coarse、30 倍目标仿真时钟；实际倍率受机器负载影响。
部署根目录运行 `bash scripts/orin/start_exploration_demo.sh`，默认独立域 73。
完整启动、关闭 RViz、手动点选、运行中调速操作见根目录 `部署可视化与Demo.md`。
