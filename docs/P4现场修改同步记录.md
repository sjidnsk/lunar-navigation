# P4 现场修改同步记录（2026-09-16）

目标分支：feat/obj-tcp-simulation，工作树 obj-tcp-simulation；开始时干净，基线 2e252dd。本次同步本机源码；后续按用户明确要求重启过 P3 建图和 P4 探索相关节点。未修改 P1/P2/P3 源码或在线算法参数。

## 内容

- 从 fix/exploration-candidate-completion 同步增量探索完成判定和 3 个回归测试：覆盖率不再触发完成；无前沿或无生成候选时正常完成；近期访问/失败抑制仍按原逻辑等待地图变化。
- tools/tcp_deployment/templates/debug/p3_joint 保存现场 P4 控制、反馈、私有 TF、常驻转发、启动脚本、参数和 RViz 配置。导航启用 TrackingStatus；2 m 起点盲区余量，0.2 m/s，终点 0.10 m / 0.05 rad，保留朝向要求。TF 超时 5 s，快速里程计超时 0.5 s。
- 导出清单包含上述目录和说明；使用既有模板机制，不增加运行模块。
- 导航器重启后 RViz 目标桥旧句柄问题仅记录操作恢复方法，未声称代码修复。
- 候选点区域外现场检查为 0，只记录现有边界语义，无新增边界算法。候选限制是任务栅格约束，不等同于路径全程受多边形约束。

## 验证

本机 Jazzy：受影响的增量探索库、节点与测试目标构建成功；保留 TCP 分支原有的两项终点朝向测试，显式加载新构建的库后最终 43/43 通过。第一次测试误加载旧 install 库使新增 3 项失败，指定当前 build 库后复测通过。本机没有 CycloneDDS，使用 Fast DDS。

TCP 仿真 Python 测试：94 passed / 7 skipped；联调反馈与转发：11 passed。Python 语法、YAML、Shell 语法、UTF-8 和 git diff --check 通过。

使用临时合成地图进行导出结构测试，233 个文件，修复和联调目录均包含，无日志、PID、锁、标定快照和缓存。该测试只验证打包结构，不是地图或仿真测试。未覆盖已有 P4 正式压缩包及校验文件，未生成新的正式地图部署包。

之前 Orin/Humble/CycloneDDS：41/41 通过；现场 84.70% 覆盖仍导航执行。该现场证据属于前一轮，不代表本次重新跑车。真实 99% 和候选最终耗尽仍 NOT_RUN。

临时验证产物在 /tmp/p4-sync-20260916，历史设备证据在仓库外 exports/P4-orin-debug-20260916。本记录随 TCP 分支源码一同提交；未合并到其他分支、未推送。

追加现场操作：暂停确认停车后，沿用原边界发送新 START。新任务 p4_candidate_exhaustion_1789538937，覆盖率 91.859375%，状态 WAITING_FOR_MAP_CHANGE，转发 PARKED；任务重建成功，但没有恢复运动，未声明探索继续行驶。

## 后续现场问题与边界

用户确认在 Unreal 重置或移动过车辆/场景。P4 继续使用旧锚定，位姿约 (-837,1317) m，而地图仍在旧区域。读取的历史导航结果均为 START_OUTSIDE_FINE_MAP；探索器将这类 NO_PATH 记作候选失败，导致 168 个候选被抑制。需要区分输入/起点异常与目标不可达；本次未实现该分类修复，也未实现位姿跳变自动恢复。

获准重启 P3 后，已建立新原点地图，但 P3 雷达配准不收敛，未持续输出有效定位。P4 新任务范围为新原点周围 80 × 80 m，最终状态 WAITING_FOR_INPUT，转发 PARKED。不能将历史 91.86% 覆盖当作新场景覆盖率。

对同样的三帧静止点云做 Orin 隔离重放：0.5 m 降采样、12 和 32 次迭代均失败；0.2 m 降采样、12 次上限下，后两帧分别在 5 和 7 次迭代收敛。该测试未改变在线 P3 参数，未证明真实定位精度、运动场景或长期稳定性。随后用户要求按原参数再重启，失败复现；P4 仍等待有效输入。本次提交不包含 P3 调参或配准代码修改。

候选失败、位姿对齐和配准的原始设备证据保留在仓库外 exports/P4-orin-debug-20260916/scene-reset-failure 与 registration-probe，未随源码提交。

## 提交前复验

2026-09-16：Python 合并运行 105 passed、7 skipped；Jazzy 构建成功。43 项节点测试首次 42 通过、1 失败，失败为已有 SuppressedCandidatesAreGrayAndActiveCandidateStaysRed 异步状态断言（状态消息尚未与候选诊断同步）；完整复跑 43/43 通过。保留该时序波动记录，不宣称多轮稳定性已验证。新文件语法、UTF-8、部署清单存在性/唯一性、无运行产物检查与 diff --check 通过。
