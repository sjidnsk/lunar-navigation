# 决策图与候选对应 Critic 实施清单

依据：`../specs/2026-09-18-drl-decision-graph-consolidated.md` 与 `../specs/2026-09-18-drl-critic-action-alignment.md`。2026-09-19 用户授权实施两项。

保持：Actor 网络参数结构、奖励、折扣、熵、导航和控制职责。保留既有训练产物，不自动长训、不提交或合并。

1. 图几何内核：合法栅格路径上的稳定 2 m cover，8 m 候选距离与 1.2 spanner；正式实际位姿接入。原生加速有界搜索，避免 Python 在百万格上逐点堆遍历。
2. 决策图适配：保留有效基础世界节点，贪心补足八方向观测接口；替换矩形门户/MST/19邻居relay；不以任务范围或零收益裁剪移动。测试开放、洞穴、薄墙、窄道、到达容差、增量扩展。
3. Critic 数据契约：每个冻结状态保存唯一候选位置、动作映射、CSR合法距离支持和每动作 g*；场景共享骨架，回放不保存射线位图。
4. 训练侧真值描述：场景初始化同规则建骨架；用真值合法起点连接取得 r+c_min 内支持；在当前冻结 K 上算全候选静态 g*，下一状态独立计算。
5. 网络与学习集成：候选只读局部几何加权真值特征与 g*/100 加入 Q；保持全局摘要与测量分支、双 Critic 和目标网络。Actor 不读特权；下一动作集仍来自测量图。
6. 协议版本与恢复：动作图及 Critic schema 更新，旧全量恢复明确拒绝；不把旧回放补零伪装成兼容，不修改旧模型文件。
7. 验证：先失败的行为测试，原生构建、包测试；月表/洞穴导航闭环、至少一次真实新结构学习更新与保存/恢复。更新设计状态和操作文档，分别标注本机 Jazzy 与 NOT_RUN 层级。

分工：图模块/原生几何独立实现；Critic契约/批处理/网络独立实现；主会话负责训练环境描述、版本、集成和文档。共享接口先固定，修改文件互斥。

实施状态（2026-09-19）：1–7项已完成实现、审查与所列功能验证，详见[实施验证](../../validation/2026-09-19-drl-graph-critic-implementation.md)。探针中另复现一例执行跟踪碰撞并保留证据；没有把它隐藏成通过，也没有扩展本轮修改导航/控制器。正式长训、收敛和部署验证未执行。
