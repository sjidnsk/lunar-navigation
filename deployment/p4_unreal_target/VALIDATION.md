# 验证记录（2026-09-17）

## 通过

- 本地纯 Python：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest deployment/p4_unreal_target/test/test_ue_path.py -q`，5 passed。验证请求/响应布局、非法输入、非单位坐标对齐往返、同会话版本过滤和 GridMap 循环缓冲高度读取。
- Orin Humble：独立 `/home/yanfa/P4/vehicle_interface` 原生 colcon 构建成功（最终增量构建58秒）；上述5项测试通过。
- Orin TCP/ROS 回环：实际编译后的 C++ 车辆接口 + 实际 P4 位姿适配进程 + 双 ROS 域181/182 + 假导航 Action；169条遥测、6次请求、2个导航目标、7条响应。验证 TCP 分片/粘包、遥测与请求复用、响应长度、非法/旧请求响应拒绝、厘米坐标往返、无目标朝向、先暂停探索、旧 Action 取消后提交新目标、UUID/路径版本隔离、路径失效清线、NaN目标拒绝。
- 回环测试只连接127.0.0.1临时端口，没有连接Unreal或使用现场控制域。
- 现场切换：扩展接口 PID365680，连接192.168.10.22:6668；只保留一条该端口连接。原接口 PID160766已正常退出。P3定位建图保持运行。
- P4导航启动成功：P3地图、反馈和新静止对齐READY，RViz已启动；状态WAITING_FOR_UE_CLICK，没有由测试代码发送现场导航目标。
- 原接口源码15个文件逐项SHA256检查不变；原接口安装保留。

## 验证边界

- 回环使用假的导航Action，验证的是适配数据链路，不替代实际规划成功/车辆到点验收。
- 本轮没有修改规划核心，因此没有重跑完整核心回归；完整项目回归NOT_RUN。
- 现场UE点击→实际规划→UE渲染→车辆到点闭环：等待操作员点击，NOT_RUN。TCP无接收确认，接口日志发送成功不代表UE已经显示。
- 提供的UE OnReceive仍假定一次回调一个完整包。Orin侧完整发送已修复；UE长路径分包/粘包可靠性未修复、未验收。
- 高程插值仅用于绘图；静止对齐并非独立地标校准，地图漂移误差仍可能造成显示偏差。

## 证据与回滚备份

远程：`/home/yanfa/P4-archives/ue-target-path-20260917-175346/`。

原运行环境保存在权限0600的original-runtime.json，不复制到Git。构建、回环、部署启动日志及源码哈希存放远程归档中。源码工作树：`fix/unreal-target-path`；未提交、未合并、未推送。

中间失败保留为开发记录：第一次回环测试夹具在一个分片包中间插入遥测，导致校验失败；第二次故意错误UUID测试误修改了假服务器原始goal_id；第三次测试的numpy uint8需显式转Python int。这三项均为测试夹具问题，修正后最终回环通过，未据此放宽产品校验。
